# swift project
*.xcuserstate
# Xcode user-specific data (should not be committed)
xcuserdata/
**/xcuserdata/
# Xcode shared data (schemes) should be committed - do not ignore xcshareddata/
swift/AudioCaptureSys/build*

# binaries
*.bin

# common
.DS_Store
.env
.env.local

# python
__pycache__/
*.pyc

# Configuration files with sensitive data
# Note: Info.plist.example is tracked, but actual Info.plist should not contain real credentials
# If you need to override Info.plist locally, create a local copy and add it here
PrivateMind/Info.plist.local

# XCFramework binaries (too large for git, build locally)
llama-cpp-xcframework/LlamaFramework.xcframework/
llama-cpp-xcframework/.build
whisper-cpp-xcframework/WhisperFramework.xcframework/
whisper-cpp-xcframework/.build